# server-first-dev bootstrap

You are connected to a fresh Ubuntu server over SSH in Cursor.

First, use the `server-first-dev` skill:

```bash
npx skills add https://github.com/Majncz/skills --skill server-first-dev
```

If `node`, `npm`, or `npx` is not installed yet, install the minimal prerequisites yourself first. If the skill still is not available in this chat, clone or read the skill from:

```text
https://github.com/Majncz/skills/tree/main/server-first-dev
```

Then follow `server-first-dev/SKILL.md`.

The server may be completely clean. Install everything needed for the app, deployment, Docker Compose, Caddy, PostgreSQL, MinIO, backups, and the server workbench.

After that, build this application:

```text
PASTE YOUR APPLICATION REQUIREMENTS HERE
```
